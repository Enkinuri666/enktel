//! M3U / M3U8 playlist parser.  A slim, streaming-tolerant implementation
//! matching what the Android `ContentRepository.parseM3u` accepts.
//! WIP — bodied out in a follow-up commit; kept as a module so the main
//! binary compiles even before the parser is fleshed out.
